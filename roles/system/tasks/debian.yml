- name: "{{ task_prefix}}Update cache"
  become: true
  apt:
    update_cache: true

- name: "{{ task_prefix}}Install utilities"
  become: true
  apt:
    state: latest
    pkg:
      - bat
      - fzf
      - jq
      - python3-venv
      - ripgrep
      - tmux
      - tree
      - vim
