- name: "{{ task_prefix}}Update cache"
  become: true
  apt:
    update_cache: true

- name: "{{ task_prefix}}Install packages"
  become: true
  apt:
    state: latest
    pkg: "{{ system_packages_debian }}"
